# Sinolink IBD7

Sinolink 投行七部 V9 HTML presentation.

Open `index.html` in a browser to view the deck. The page supports keyboard navigation with arrow keys and page navigation with the `?slide=` query parameter.
